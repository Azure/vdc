{
    "deployment-prefix": {
        "value": "${general.organization-name}-${on-premises.deployment-name}"
    },
    "subnets": {
        "value": "${on-premises.network.subnets}"
    },
    "vnet-address-prefix": {
        "value": "${on-premises.network.address-prefix}"
    },
    "vnet-dns-servers": {
        "value": "${on-premises.network.dns-servers}"
    }
}